export interface LoginInfo {
  email: string;
  password: string;
}

export interface UserInfo {
  id: number;
  email: string;
  name: string;
  gender: string;
  birthday: number;
  profileImg: string;
}
